name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [guitarshop-backend, react-frontend]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          cd ${{ matrix.project }}
          # Try strict install first, fall back to a clean npm install if optional/native deps fail
          npm ci || (
            rm -rf node_modules package-lock.json || true
            npm cache clean --force || true
            npm install
          )
          # Attempt to rebuild any native bindings that might be missing
          npm rebuild || true

      - name: Lint (strict)
        run: |
          cd ${{ matrix.project }}
          # Fail on any lint warnings
          npx eslint . --max-warnings=0

      - name: Build
        run: |
          cd ${{ matrix.project }}
          npm run build

      - name: Run tests
        run: |
          cd ${{ matrix.project }}
          if [ -f package.json ]; then
            if npm run | grep -q "test"; then
              npm run test || true
            else
              npx vitest run || true
            fi
          fi
